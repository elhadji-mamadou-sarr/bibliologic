{% extends 'base.html.twig' %}

{% block title %}LigneCommande{% endblock %}

{% block body %}
    <h1>LigneCommande</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ligne_commande.id }}</td>
            </tr>
            <tr>
                <th>Designation</th>
                <td>{{ ligne_commande.designation }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ ligne_commande.prix }}</td>
            </tr>
            <tr>
                <th>PremierPaiement</th>
                <td>{{ ligne_commande.premierPaiement ? ligne_commande.premierPaiement|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Domaine</th>
                <td>{{ ligne_commande.domaine }}</td>
            </tr>
            <tr>
                <th>Rubriques</th>
                <td>{{ ligne_commande.rubriques ? ligne_commande.rubriques|join(', ') : '' }}</td>
            </tr>
            <tr>
                <th>InformationUtil</th>
                <td>{{ ligne_commande.informationUtil }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ligne_commande_index') }}">back to list</a>

    <a href="{{ path('app_ligne_commande_edit', {'id': ligne_commande.id}) }}">edit</a>

    {{ include('ligne_commande/_delete_form.html.twig') }}
{% endblock %}
